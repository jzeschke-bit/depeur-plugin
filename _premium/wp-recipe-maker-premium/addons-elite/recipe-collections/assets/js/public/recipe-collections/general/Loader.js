import React from 'react';

import '../../../../css/public/loader.scss';

const Loader = (props) => {
    return (
        <div className="wprm-collections-loader"></div>
    );
}
export default Loader;